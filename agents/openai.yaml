interface:
  display_name: "Coverage-Gated Refactor"
  short_description: "Enumerate module->parts, interactive-select scope, then uninterrupted gated refactor"
  default_prompt: "Capture user refactor goal and scope first, enumerate all changeable modules and parts, use interactive CLI selection for large candidate sets, lock final selected modules and parts, then execute uninterrupted with 90 percent module coverage gates, module-level regression checks, and documentation updates until all selected work is complete."
